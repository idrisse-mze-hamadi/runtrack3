document.addEventListener("DOMContentLoaded", (event) => {
    
    var button = document.getElementById("trier")

    button.addEventListener('click', function(){
        fetch('stat.json')
        
    })

})